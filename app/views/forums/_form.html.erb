<%= form_for(@forum) do |f| %>
  <% if @forum.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@forum.errors.count, "error") %> prohibited this forum from being saved:</h2>

      <ul>
      <% @forum.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :characterName %><br>
    <%= f.text_field :characterName %>
  </div>
  <div class="field">
    <%= f.label :characterPicture %><br>
    <%= f.text_field :characterPicture %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
